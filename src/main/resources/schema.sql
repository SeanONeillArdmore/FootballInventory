CREATE TABLE jersey (id VARCHAR(255),
                        size VARCHAR(255),
                        team VARCHAR(255),
                        year INTEGER,
                        color VARCHAR(255));

ALTER TABLE jersey ADD PRIMARY KEY (id);